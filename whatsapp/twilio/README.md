# WhatsApp via Twilio

Put your interactive test script here, e.g. `app.py`, and any small `client.py` wrapper.

See parent `../README.md` for provider choice and env vars.

When you add code, document the exact `whatsapp:+...` “from” format for your Twilio sandbox vs production sender.
